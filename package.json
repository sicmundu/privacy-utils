{
  "name": "privacy-utils",
  "version": "0.1.0",
  "description": "Modular TypeScript library for differential privacy, secure aggregation, and anonymous authentication",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:browser": "turbo test:browser",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "changeset": "changeset",
    "release": "pnpm changeset publish",
    "version": "pnpm changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "turbo": "^2.0.0"
  }
}
